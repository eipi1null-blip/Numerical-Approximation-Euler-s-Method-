# Euler Method Slope Field Applet

Upload these files to the repository root.

The root `index.html` is the complete applet. It shows Euler-method approximations for `y' = x - y^2`, `y(1) = 1.5`, comparing step sizes from `x = 1` to `x = 2`.

